## WOD Timer — Stage 1 (Tabata)

Web-based Tabata interval timer (work/rest/rounds) built with Vite + vanilla JS.

### Run locally

```bash
npm install
npm run dev
```

Then open the URL shown in the terminal.

### Build / preview

```bash
npm run build
npm run preview
```

